query GetRoomById($roomTypeId: Int!, $fetchFromCdb: Boolean) {
    Room(roomTypeID:$roomTypeId, fetchFromCDB: $fetchFromCdb) {
        id
        dmcRoomId
        allotmentAutoTopup
        noOfRoom
        hotelRoomTypeId
        inventoryTypeId
    }
}